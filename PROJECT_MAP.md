# 🌳 skeletree — Project Map

> **map ≈ 2.8K tokens · full-read baseline ≈ 22.0K · ~87% smaller**
>
> Signatures only — no function bodies. Generated by `skeletree`.

## Tree

```text
skeletree/
  src/
    skeletree/
      extractors/
        __init__.py
        base.py
        python_ast.py
        treesitter.py
      render/
        __init__.py
        json_out.py
        markdown.py
      __init__.py
      cache.py
      cli.py
      config.py
      core.py
      model.py
      tokens.py
      walker.py
  tests/
    test_cache.py
    test_cli.py
    test_config.py
    test_python_ast.py
    test_render.py
    test_tokens.py
    test_treesitter.py
    test_walker.py
  CONTRIBUTING.md
  LICENSE
  pyproject.toml
  README.md
  ultra-think-i-want-humming-sunbeam.md
```

## Symbols

### `src/skeletree/cache.py` · python

- `const CACHE_FILENAME`
- `const CACHE_VERSION`
- `class Cache` — Load-once, query-by-stat, write-once cache of FileEntry objects.
  - `__init__(self, root: Path, enabled: bool=True)`
  - `load(self) -> None`
  - `get(self, rel: str, size: int, mtime: float) -> FileEntry | None` — Return the cached entry iff size and mtime both match.
  - `put(self, entry: FileEntry) -> None` — Record an entry to be persisted on ``save``.
  - `save(self) -> None`
- `_mtime_eq(a: float, b: float) -> bool`

### `src/skeletree/cli.py` · python

- `class DefaultCommandGroup(TyperGroup)` — Run the ``map`` command when no subcommand is given.
  - `parse_args(self, ctx, args)`
- `_force_utf8() -> None` — Ensure stdout/stderr can emit the map's Unicode on any platform.
- `_version_callback(value: bool) -> None`
- @app.command('map') `map_command(path: Path=…, out: str=…, fmt: str=…, max_files: int | None=…, no_cache: bool=…, quiet: bool=…, _version: bool=…) -> None` — Generate the project map (this is the default — runs with no subcommand).
- `_run_map(path: Path, *, out: str | None, fmt: str | None, max_files: int | None, no_cache: bool, quiet: bool) -> None`
- `_emit(output: str, out: str, root: Path) -> None`
- @app.command() `init(path: Path=…) -> None` — Wire the map into Claude Code: add a CLAUDE.md pointer, print a hook snippet.

### `src/skeletree/config.py` · python

- `const DEFAULT_IGNORE_DIRS: frozenset[str]`
- `const DEFAULT_COLLAPSE_THRESHOLD`
- @dataclass(frozen=True) `class Config` — Resolved configuration. Frozen so it can be passed around freely.
  - @property `ignore_dirs(self) -> frozenset[str]`
  - @classmethod `load(cls, root: Path) -> Config` — Read ``[tool.skeletree]`` from pyproject.toml or .skeletree.toml.
  - `merged_with_cli(self, *, out: str | None=None, fmt: str | None=None, max_files: int | None=None, no_cache: bool=False) -> Config` — Layer explicit CLI flags on top of file config (CLI wins).
- `_read_table(root: Path) -> dict[str, Any]`

### `src/skeletree/core.py` · python

- @dataclass `class BuildStats`
- `build_map(root: Path, config: Config) -> tuple[ProjectMap, BuildStats]`
- `_entry_for(path: Path, root: Path, cache: Cache, stats: BuildStats) -> FileEntry | None`
- `_parse(path: Path, rel: str, size: int, mtime: float) -> FileEntry | None`

### `src/skeletree/extractors/__init__.py` · python

- `const PYTHON_EXTENSIONS`
- `language_for(ext: str) -> str` — Return the language name for an extension, or 'unknown'.
- `supported_extensions() -> set[str]`
- `extract_file(ext: str, source: str) -> tuple[str, list[Symbol]]` — Extract symbols for a file by extension. Returns (language, symbols).

### `src/skeletree/extractors/base.py` · python

- `class Extractor(Protocol)` — Anything that can turn source into a list of top-level symbols.
  - `extract(self, source: str) -> list[Symbol]` — Return top-level symbols (with nested children). Must not raise on

### `src/skeletree/extractors/python_ast.py` · python

- `extract(source: str) -> list[Symbol]`
- `_body_symbols(body: list[ast.stmt], *, top_level: bool) -> list[Symbol]`
- `_class_symbol(node: ast.ClassDef) -> Symbol`
- `_func_symbol(node: ast.FunctionDef | ast.AsyncFunctionDef, *, kind: str) -> Symbol`
- `_const_symbol(node: ast.Assign | ast.AnnAssign) -> Symbol | None`
- `_format_args(args: ast.arguments) -> str`
- `_default(node: ast.AST) -> str` — Render a default value, collapsing long/complex ones to '…'.
- `_arg(arg: ast.arg) -> str`
- `_docstring(node: ast.AST) -> str`
- `_expr(node: ast.AST | None) -> str` — Render an expression back to compact source for signatures/decorators.

### `src/skeletree/extractors/treesitter.py` · python

- `const EXTENSION_LANGUAGE: dict[str, str]`
- @dataclass(frozen=True) `class LangSpec` — How to recognize declarations in one language's syntax tree.
- `const SPECS: dict[str, LangSpec]`
- `const _parser_cache: dict[str, object]`
- `const _AVAILABLE: bool | None`
- `is_available() -> bool` — Whether the tree-sitter stack imported. Cached; safe to call often.
- `language_for_extension(ext: str) -> str | None`
- `extract(source: str, language: str) -> list[Symbol]` — Extract symbols for one of the tree-sitter languages.
- `_get_parser(language: str)` — Build a *standard* tree_sitter.Parser for the language.
- `_collect(node, spec: LangSpec) -> list[Symbol]` — Walk a node's named children, classifying declarations.
- `_make_container(node, spec: LangSpec, kind: str) -> Symbol`
- `_make_function(node, spec: LangSpec, kind: str) -> Symbol`
- `_make_simple(node, kind: str) -> list[Symbol]` — One declaration node may carry several names (Go ``const (...)`` blocks,
- `_name_of(node) -> str`
- `_find_identifier(node) -> str`
- `_find_descendant(node, types: frozenset[str], *, stop: frozenset[str])` — First descendant whose type is in ``types``, not descending into ``stop``.
- `_body_of(node)`
- `_heritage(node) -> str` — Best-effort superclass / generics text for a container, compacted.
- `_text(node) -> str`
- `_squash(text: str) -> str` — Collapse whitespace/newlines in a signature to a single clean line.

### `src/skeletree/model.py` · python

- @dataclass `class Symbol` — A single top-level (or nested) declaration: signature, never a body.
  - `to_dict(self) -> dict[str, Any]`
  - @classmethod `from_dict(cls, d: dict[str, Any]) -> Symbol`
- @dataclass `class FileEntry` — One parsed file: its symbols plus the stat used for cache invalidation.
  - `to_dict(self) -> dict[str, Any]`
  - @classmethod `from_dict(cls, d: dict[str, Any]) -> FileEntry`
- @dataclass `class DirNode` — A node in the rendered directory tree.
- @dataclass `class ProjectMap` — The whole product: root name, tree, and every file entry.
  - `to_dict(self) -> dict[str, Any]`

### `src/skeletree/render/json_out.py` · python

- `render(project: ProjectMap, savings: TokenSavings) -> str`

### `src/skeletree/render/markdown.py` · python

- `render(project: ProjectMap, headline: str) -> str`
- `_tree_lines(root: DirNode) -> list[str]`
- `_walk_tree(node: DirNode, depth: int, lines: list[str]) -> None`
- `_collapsed_child(node: DirNode) -> int | None`
- `_file_section(entry: FileEntry) -> list[str]`
- `_render_symbol(sym: Symbol, depth: int, lines: list[str]) -> None`
- `_symbol_text(sym: Symbol) -> str`
- `_declaration(sym: Symbol) -> str`
- `_clip(text: str) -> str`

### `src/skeletree/tokens.py` · python

- `const CHARS_PER_TOKEN`
- `estimate_tokens(text: str) -> int` — Approximate token count for a string (chars/4, rounded).
- @dataclass `class TokenSavings` — The before/after the README and CLI both lead with.
  - @property `saved_tokens(self) -> int`
  - @property `percent_smaller(self) -> int`
  - `headline(self) -> str` — One-line summary, e.g. 'map ≈ 5.1K tokens · full-read baseline ≈ 51K · ~90% sma…
- `compute_savings(project: ProjectMap, rendered_map: str) -> TokenSavings` — Compare the rendered map's size against reading every file in full.
- `estimate_tokens_from_chars(char_count: int) -> int`
- `_human(n: int) -> str` — Compact human form: 512 -> '512', 5123 -> '5.1K', 1_200_000 -> '1.2M'.

### `src/skeletree/walker.py` · python

- `_load_gitignore(root: Path) -> pathspec.PathSpec | None` — Compile the repo's root .gitignore into a matcher, if present.
- `_rel_posix(path: Path, root: Path) -> str`
- `walk(root: Path, config: Config) -> Iterator[Path]` — Yield files under ``root`` worth mapping, in stable sorted order.
- `_is_skippable_file(entry: os.DirEntry) -> bool`
- `build_tree(rel_paths: list[str], root_name: str, collapse_threshold: int) -> DirNode` — Build the renderable directory tree from a flat list of relative paths.
- `_find_dir(node: DirNode, name: str) -> DirNode | None`
- `_sort_tree(node: DirNode) -> None`
- `_collapse(node: DirNode, threshold: int) -> None`

### `tests/test_cache.py` · python

- `_repo(root: Path, n: int=3) -> None`
- `test_second_run_uses_cache(tmp_path)`
- `test_touching_one_file_reparses_only_it(tmp_path)`
- `test_no_cache_reparses_everything(tmp_path)`
- `test_cache_file_written(tmp_path)`

### `tests/test_cli.py` · python

- `_repo(root: Path) -> None`
- `test_map_writes_project_map(tmp_path)`
- `test_map_json_format(tmp_path)`
- `test_map_stdout(tmp_path)`
- `test_bad_format_errors(tmp_path)`
- `test_missing_directory_errors()`
- `test_version()`
- `test_init_creates_claude_md(tmp_path)`
- `test_init_is_idempotent(tmp_path)`
- `test_init_appends_to_existing_claude_md(tmp_path)`

### `tests/test_config.py` · python

- `test_defaults(tmp_path)`
- `test_loads_from_pyproject(tmp_path)`
- `test_standalone_toml_takes_priority(tmp_path)`
- `test_malformed_toml_falls_back(tmp_path, capsys)`
- `test_cli_overrides_win()`
- `test_default_ignore_dirs_includes_common_noise()`

### `tests/test_python_ast.py` · python

- `_names(symbols)`
- `test_extracts_functions_with_signatures()`
- `test_async_function_marked_in_signature()`
- `test_class_with_methods_and_bases()`
- `test_decorators_captured()`
- `test_upper_case_constants_only()`
- `test_long_default_collapsed()`
- `test_varargs_and_kwargs()`
- `test_syntax_error_returns_empty()`
- `test_no_bodies_leak()`

### `tests/test_render.py` · python

- `_project()`
- `test_markdown_has_headline_tree_and_symbols()`
- `test_markdown_async_method_rendered()`
- `test_markdown_callable_has_no_kind_word()`
- `test_markdown_const_tagged()`
- `test_markdown_no_bodies()`
- `test_collapsed_dir_rendered_as_summary()`
- `test_json_round_trips_and_has_savings()`

### `tests/test_tokens.py` · python

- `test_estimate_is_chars_over_four()`
- `test_savings_headline_and_percent()`
- `test_human_formatting_in_headline()`
- `test_no_negative_savings()`

### `tests/test_treesitter.py` · python

- `_flat_names(symbols)`
- `test_javascript_functions_and_classes()`
- `test_typescript_interface_and_typed_function()`
- `test_go_funcs_methods_and_types()`
- `test_rust_struct_and_impl()`
- `test_java_class_methods()`
- `test_ruby_class_and_methods()`
- `test_no_function_bodies_leak()`
- `test_unparseable_returns_empty()`

### `tests/test_walker.py` · python

- `_make_repo(root: Path) -> None`
- `_rel(paths, root)`
- `test_walk_respects_defaults_and_gitignore(tmp_path)`
- `test_walk_is_deterministic(tmp_path)`
- `test_extra_ignore_globs(tmp_path)`
- `test_build_tree_collapses_large_dirs()`
- `test_build_tree_orders_dirs_before_files()`
